<%
  # Downcase all information
  dom = scope.lookupvar('::domain').downcase
%>

### Torque client from Puppet template
# Domain: <%= @dom %>
# Registered on: <%= @h %>
<%= @hostname %> np=<%= @cpus %>